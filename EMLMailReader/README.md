# emlmailreader regress

This is a copy of the emlmailreader package contents, modified to work with
Python 3.8.

The only changes made to the original are the elimination of function return
type hints which Python 3.8 considers syntax errors. Everything else works as
advertised.

